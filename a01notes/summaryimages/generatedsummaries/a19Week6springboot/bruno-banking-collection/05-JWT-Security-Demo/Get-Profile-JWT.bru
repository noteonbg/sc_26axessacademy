meta {
  name: Get Account Profile (JWT Bearer Auth)
  type: http
  seq: 2
}

get {
  url: http://localhost:8085/api/v1/account/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}
